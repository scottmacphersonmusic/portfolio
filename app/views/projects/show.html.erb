<h2><%= @project.name %></h2>
<p>Technologies used: <%= @project.technologies_used %></p>
<%= image_tag @project.image_url, class: "project_image" %>
<%= link_to "Back", projects_path %>
<%= link_to "Destroy", @project, method: :delete, data: { confirm: 'Are you sure?' } %>
